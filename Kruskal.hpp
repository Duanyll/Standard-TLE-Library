//这个不好用LFS存啊
/*
// 把所有边排序，记第i小的边为e[i] (1<=i<=m)m为边的个数 
// 初始化MST为空
// 初始化连通分量，使每个点各自成为一个独立的连通分量

for (int i = 0; i < m; i++)
{
    if (e[i].u和e[i].v不在同一连通分量)
    {
        // 把边e[i]加入ＭＳＴ
        // 合并e[i].u和e[i].v所在的连通分量 
    } 
}
*/